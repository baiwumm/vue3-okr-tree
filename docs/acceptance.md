# 验收证据表（acceptance）

> 建表日期 **2026-09-21**。方法：不引用任何文档结论，直接对源码、产物与真实 run 重测——`grep` 判据落点、`find` 数基线、`gh run list` 查历史、跑门禁取数字。
> 结构与姊妹包 `react-okr-tree/docs/acceptance.md` 对齐，便于两端口径互查。
> 门禁数字的完整快照另见 `docs/release-readiness.md`「当前状态」（2026-09-21 已复跑对账），本文只引用、不重复维护。

## 1. 一句话结论

代码与门禁已达发布条件并且**已真实上线**：1.13.0 于 2026-09-21 `06:29:10Z` 手动首发，226 条单测、覆盖率四项、size 三项、attw 四格全绿，`ci.yml` / `visual.yml` / `release.yml` 三条 workflow 最近一次均 success。

**2026-09-22 更新**：**1.14.0 已由 CI 经 OIDC Trusted Publishing 真实发布**（run `35708582755`，provenance 已入 sigstore），第 5 节里原先「尚未被验证的一步」就此收口。本包至此完整跑通了「tag 触发 → 门禁 → OIDC 发包 → provenance → GitHub Release」全链路。

**2026-09-24 更新**：**1.14.1 已发布**（patch，对外 API 零变化）。发布这一批同时把四条 workflow 的 action 从仍在 Node 20 上跑的 `v4` 升到第一个脱离 node20 的档位（checkout / setup-node / pnpm 三大 action → v5，upload-artifact → v6，逐个拉各 major 的 action.yml 实测 `runs.using` 后定的档），CI run 日志里的弃用警告 5 行 → 0 行、Visual 1 行 → 0 行，两仓四条 run 全绿。

`requirements.md` 第 7 节的 5 条验收项：~~**4 条 ✅、1 条 ⚠️**（剩第 2 条 Demo 项）~~ → **5 条全部 ✅**（2026-09-25，G8 的 Demo 交互层断言收口）；第 6 节 Q1–Q9：**9 条全部 ✅**。

> 更正一处本文首版的计数错误：当时写成「4 ✅ 1 ⚠️」，但按第 2 节的表实际是 3 ✅ 2 ⚠️（第 2 条与第 5 条都是 ⚠️，我漏看了第 5 条）。本次 Q8 收口让第 5 条转 ✅，两种口径才恰好重合。

**没有出现「文档声称有、实际查无」的功能级假 ✅**——所有缺口都是「行为在、断言缺」。

> **同日增量（建表之后）**：原先唯一「线上真实行为无守卫」的 Q9 已收口——`verify:dist` 现用 `createRequire` 真实加载 `dist/vue3-okr-tree.cjs` 并断言 3 项（见第 3 节 Q9 行与第 6 节 G5）；`BUILT_IN_THEMES` 已从包入口导出，与 react 侧导出面对齐（G7）；`requirements.md` 的 Demo 用例数 20 已更正为 24（G1）。因此本文的 assert 总数、导出数等数字均已按重跑结果刷新。

## 2. 验收标准 1–5（`requirements.md` 第 7 节）

| 条目                                                                   | 状态 | 证据                                                                                                                                                                                                                                                                                                                                                    | 缺口                                                                                                                                                                |
| ---------------------------------------------------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1（3.1–3.4 全部 API 行为一致，含抛错文案语义）                         | ✅   | `tests/` 内 11 条 `toThrow`，文案逐字断言：`[Tree] leftData is required in onlyBothTree`、`[Tree] nodeKey is required in setCurrentNode` / `getCurrentNode`、`[Tree] filterNodeMethod is required when filtering`；组件层 `okr-tree.spec.ts` 27 条、`query-methods.spec.ts` 6 条、`expand-methods.spec.ts` 10 条                                        | —                                                                                                                                                                   |
| 2（Demo 用例全部可交互、与原 Demo 对齐；Filter 体现空值恢复）          | ✅   | `playground/components/demos/` 实有 **24** 个 `.vue`，全部注册并渲染于 `playground/App.vue:30-76`（import 见 `:91-114`）；`tests/visual/visual.spec.ts` 10 条截图用例覆盖主要形态；**24 个用例的交互结果由 `tests/visual/demo-interaction.spec.ts` 22 条浏览器用例逐个断言**（2026-09-25 收口 G8）；Filter 空值恢复见 `tree-store.spec.ts` 的 Q1 组用例 | ~~「20 个用例」的说法已过时（现 24）~~ **已更正**；~~Demo 交互本身无自动化断言，靠像素基线兜底~~ **已收口**：本批查出并修掉 4 处 Demo 自身缺陷（详见第 6 节 G8 行） |
| 3（构建产物齐备：es / umd / cjs / style.css / 类型声明）               | ✅   | `dist/` 9 个文件（`vue3-okr-tree.es.js` / `.cjs` / `.umd.js` / `style.css` / `index.d.ts` / `index.d.cts` + 3 张 map）；`verify:dist` 62 条 assert；`verify:package` → publint 通过 + attw 四格 🟢（node10 / node16 CJS / node16 ESM / bundler）；`size` 三项均达标                                                                                     | CSS 余量仅 4%（3.84 / 4 kB gzipped）                                                                                                                                |
| 4（原仓库已知 bug 场景正常：异步改 data、左右子树单独更新、倒序显示）  | ✅   | 倒序显示：`tests/model/node.spec.ts:57`「保持源数据顺序（倒序显示 bug 场景）」；异步改 data：`tests/components/perf.spec.ts:63-81` 两条（`deep-watch: false` 时原地变更不触发更新、默认保持增量更新）+ `node.spec.ts:242/256`；左右子树单独更新：`prop-sync.spec.ts`「leftData 运行时变更：重建左树后按 expanded-keys / current-key 恢复左树状态」      | —                                                                                                                                                                   |
| 5（Q1–Q5、Q8 各有用例覆盖；Q3 复刻项有单测验证源数据副作用与原版一致） | ✅   | Q1–Q5 ✅、Q3 复刻 ✅、Q8 ✅（见第 3 节）                                                                                                                                                                                                                                                                                                                | —（Q8 已随 `is-hidden` 切换断言收口，见第 3 节与 G4）                                                                                                               |

## 3. Q1–Q9 原版怪癖决策与证据（`requirements.md` 第 6 节）

| 编号 | 决策            | 覆盖 | 证据（文件:行 + 用例名）                                                                                                                                                                                                                                                                                                                                                                                                      | 缺口说明                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---- | --------------- | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Q1   | 修复            | ✅   | `tests/model/tree-store.spec.ts:66` `describe('filter（Q1：全树过滤）'`，含 `:67` 隐藏不匹配节点且父节点保持可见、`:95` 第一层节点自身参与过滤（原版永不隐藏第一层）、`:101` 多根 data 时第二个根的子树也被过滤                                                                                                                                                                                                               | —                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Q2   | 修复            | ✅   | `tree-store.spec.ts:162` 左右分表、同 key 不互相覆盖、`getNode` 右树优先；`:171` `defaultExpandedKeys` 对左右两树同时生效；`:183` 左树过滤只作用于左树；`:222` `setCurrentKey` 双树。旁证 `draggable.spec.ts:304-305`、`a11y-group.spec.ts:189`                                                                                                                                                                               | —                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Q3   | 复刻            | ✅   | `tests/model/node.spec.ts:151` `describe('增删改（Q3：同步修改源数据，复刻原版行为）'`：`:152` append、`:171` insertBefore / insertAfter（断言 `data[0].children`）、`:181` remove、`:215` updateChildren。旁证 `frozen-data.spec.ts:74` 冻结数据 append / remove 只警告不抛错                                                                                                                                                | —                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Q4   | 修复            | ✅   | `node.spec.ts:230` `describe('setData / updateChildren（Q4 与异步改 data）'`，核心 `:242` data 引用不变、原地 push 时增量更新且不报错（`not.toThrow`）、`:256` 原地删除、`:266` 同 key 换引用复用节点保留 expanded；组件层 `okr-tree.spec.ts:445`                                                                                                                                                                             | —                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Q5   | 修复            | ✅   | `tests/components/transition-robustness.spec.ts:117` 断言 `--okr-anim-duration: 50ms`，即 prop → TreeStore → CSS 变量整条链路已通；Demo `Base061.vue:23-34` 有可调 duration 输入                                                                                                                                                                                                                                              | —                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Q6   | CSS 全量迁移    | ✅   | 六组内置动画名定义在 `src/types/index.ts:14-21` 的 `AnimateName` 联合类型（`okr-fade-in-linear` / `okr-fade-in` / `okr-zoom-in-center` / `okr-zoom-in-top` / `okr-zoom-in-bottom` / `okr-zoom-in-left`），CSS 侧在 `src/lib/okr-tree/model/transition.css` 与 `style.css:625-653`；测试侧只有 `okr-fade-in` 一个名字进断言（`transition-robustness.spec.ts:121`），`reduced-motion.spec.ts:58` 仅断 `startsWith('okr-anim-')` | **已收口**：`transition-robustness.spec.ts` 用 `it.each` 对六种 `animateName` 逐个断 `okr-anim-<name>` 接线，`verify-dist.mjs` 的 CSS 侧改为逐个断 `enter-active` / `leave-active`（原先只钉 `okr-zoom-in-center` 一组）。Demo `Base061.vue:57-64` 列全 6 个但属人工点选。注：`transition.css` 里实有 **7** 个 `-enter-active` 选择器，多出的 `.okr-list` 是 `TransitionGroup` 的列表移动过渡、不属于 `AnimateName`，比对时别当成第 7 组动画 |
| Q7   | 不移植          | ✅   | **已收口**：新增 `tests/model/framework-boundary.spec.ts`，对 `src/lib/okr-tree/model/*.ts` 逐个断无 Vue 2 残留写法（`new Vue(` / `eventBus` / `$on` `$off` `$emit` / `$set` `$children` `$listeners` `$refs` / `Vue.prototype`），并断模型层从 `vue` 只引入响应式 API（当前仅 `shallowReactive`）                                                                                                                            | 文件命名与 react 侧的 `framework-boundary.spec.ts` 一致，但断言内容不同：react 那条断「模型层不引用框架」，而 vue3 的 `node.ts` 刻意用了 `shallowReactive`，照搬会写成假测试                                                                                                                                                                                                                                                                 |
| Q8   | 修复            | ✅   | **已收口**：`transition-robustness.spec.ts` 新增用例——点击 `.org-chart-node-btn` 收起时断子容器切上 `is-hidden`、按钮失去 `expanded` 类，且折叠子树节点仍在 DOM 中（`labels` 仍含「研发-前端」，正是 `getVisibleNodes` 与 DOM 节点数不等的原因），再点恢复。原有接线断言 `:120-122`、`reduced-motion.spec.ts:55-73` 保留                                                                                                      | 过渡「真的发生了动画」仍只由类名与 CSS 变量证明；像素层面 `visual.spec.ts:33` 只截落定态                                                                                                                                                                                                                                                                                                                                                     |
| Q9   | 额外产出 `.cjs` | ✅   | **已补常驻验证**：`scripts/verify-dist.mjs` 末尾用 `createRequire(import.meta.url)` 真实加载 `dist/vue3-okr-tree.cjs`，3 条断言——`OkrTree === VueOkrTree`（导出指向同一实现）、`BUILT_IN_THEMES` 为 6 项、`createTypedOkrTree` / `TreeStore` 可取。`pnpm verify:dist` 实跑 `[verify:dist] ALL PASSED`（62 条 assert）                                                                                                         | 建表时此处为 ❌（`verify-dist.mjs` 全文没有 `require(` / `createRequire`，只 `existsSync(umd)`），同日已收口；`"type": "module"` 下 `require()` 只能走 `.cjs`，这条路径现在由门禁守着而非人工记录                                                                                                                                                                                                                                            |

## 4. 对外 API 面与 react-okr-tree 的对齐（2026-09-21 双向差集实测）

比对方法：props 取 `require('./dist/vue3-okr-tree.cjs').VueOkrTree.props` 的键全集，与 react 侧 `packages/react-okr-tree/src/OkrTree.tsx:59-191` 的 `OkrTreeProps` 字段做双向差集；导出面取两边 `dist/*.cjs` 的 `Object.keys()`。

| 对比                   | 结果                                                                                                                                                                                                                                                                                                                                                 |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| props 数量             | vue3 **40** 个 ↔ react **60** 个                                                                                                                                                                                                                                                                                                                     |
| 仅 vue3 有（react 缺） | **空集**。40 个 prop 逐个在 react 存在，含原版两处拼写 `currentLableClassName`、`showCollapsable` 两边一致                                                                                                                                                                                                                                           |
| 仅 react 有            | 20 个，全部是框架映射而非新增能力：12 个 emits → 12 个 `on*` 回调（逐条 1:1，`node-click`→`onNodeClick` … `node-drop`→`onNodeDrop`）；`onExpandedKeysChange` / `onCurrentKeyChange`（D3 受控写法）；`renderNode` / `renderExpandBtn` / `empty` / `children`（D4 插槽改 render props）；`className` / `style`（requirements 4.1 明写的 React 侧透传） |
| 导出面                 | vue3 **18** 个 ↔ react **19** 个，共有 15 个。仅 vue3：`createTypedOkrTree`（react 按 D5 移除）、`VueOkrTree` 与 `VueOkrTreePlugin`（按 D6 移除，默认导出改为 `OkrTree`）。仅 react：`DEFAULT_PROPS` 与三个测试辅助导出（`resetNodeIdSeed` / `resetWarnings` / `warn`）。`BUILT_IN_THEMES` 原先只在 vue3 侧未导出，**本次已补**（见第 6 节 G7）      |
| 结论                   | **功能面已对齐，无漂移**                                                                                                                                                                                                                                                                                                                             |

> 这次比对曾暴露本仓库一处文档与实现不符：`requirements.md` 4.7 声称 `BUILT_IN_THEMES`「对齐 `src/lib/index.ts`」，但该常量当时只在 `src/types/index.ts:24` 定义、未从入口导出（react 侧导出了）。**同日已按「补导出」收口**——选择补导出而非改措辞，是为了与 react 端导出面对齐；这是新增对外 API，故记在 `CHANGELOG.md` 的 Unreleased 段，下个版本号按 minor 走（该段已随 1.14.0 定版）。
>
> 剩余的一处导出面不对称是 `DEFAULT_PROPS`（react 导出、vue3 未导出），属未决项而非缺陷，需要时再定。

## 5. 门禁与发布链路现状（2026-09-21 实跑）

| 项                                   | 结果                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm test`                          | **264** passed（25 个 spec 文件）。建表时为 226 passed / 21 文件，与 react 侧审计「重数 Vue 侧逐文件 `it()` 恰好 226」对上；1.14.0 断言补全批 +16 → 242，第 2 轮外部审计修复批（2026-09-24）再 +15 → 257（末两条是 3.4「平移后吞点击」的两条用例），第 3 轮首批 +3 → 260、第二批 +4 → **264**（拖拽 DOM 写入落点 1 + 两种锁定强度 3）                                                                                                                                                                                                                                                                                                        |
| `pnpm test:coverage`                 | 90.85 / 81.81 / 88.94 / 93.56（stmts / branch / funcs / lines），阈值 80 / 75 / 80 / 80（`vite.config.ts:66-69`）。建表时 90.69 / 81.59 / 88.94 / 93.43                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `pnpm size`                          | ESM **16.12** / 19 kB、样式 3.84 / 4 kB、UMD **16.23** / 19.5 kB（gzip），三项均达标。ESM/UMD 双双比原值小 0.5 kB 左右：压缩器由 esbuild 换成 Terser（`build.minify: 'terser'`），换的理由是产物必须保住 html-to-image 那三条 ignore 注释，见 `verify:dist` 与 `vite.config.ts` 注释                                                                                                                                                                                                                                                                                                                                                         |
| `pnpm verify:package`                | publint `All good!`；attw `No problems found`（四格 🟢）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `pnpm verify:dist`                   | 实跑 **62** 条 `ok`（22 → 25 加 `.cjs` 三条 → 34 加 CSS 六组动画逐个断言与 UMD 执行三条 → 41 加可选 peer 门禁七条：ESM / `.cjs` / `.umd` 各两条「未静态引入」+「保住三家 ignore 注释」，再加一条「ESM 仍有动态 import 调用点」→ **62** 加 CSS 几何常量 21 条，react 侧清单 G17 的轻量分支，见第 6 节末尾）                                                                                                                                                                                                                                                                                                                                   |
| 视觉基线                             | win32 14 张 + linux 14 张（`find tests/visual -name '*-linux.png'`）；`pnpm test:visual` 共 **43** 条用例（2026-09-25 起：像素与既有用例 21 条 + Demo 交互层 22 条，本批未新增任何 png）                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| workflow 最近一次                    | `ci.yml` success、`visual.yml` success、`release.yml` success                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 发布链路首跑                         | run `35579119550`（推 `v1.13.0` tag 触发）：门禁全跑 → 守卫检测到 1.13.0 已发布 → publish 步 `skipped` → `gh release create` 建出 GitHub Release；registry 版本号未变动（这次演练不发包，也没产生 provenance）                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **OIDC 端到端（2026-09-22 已验证）** | **1.14.0 由 CI 真实发布**：run `35708582755` 的 publish 步 success，日志 `+ vue3-okr-tree@1.14.0` 与 `Provenance statement published to transparency log: …logIndex=2908846083`；`gh secret list` 为空 → 无 token 参与；registry `dist-tags.latest=1.14.0`，attestations 端点返回 publish 证明，`npm audit signatures` 的 `invalid` / `missing` 均为空数组。react-okr-tree 同号跟随（run `35711304130`）；**1.14.1 已沿同链路复跑**（2026-09-24：vue3 run `36011667878` / react run `36011695748`，两条 publish 步均 success、provenance 入 sigstore，logIndex `2939652824` / `2939668829`，GitHub Release 正文取到 CHANGELOG 的 1.14.1 段） |

## 6. 已知缺口与建议顺序

按修复代价从小到大排。与 react 侧的 G 表同构，其中 G1 / G2 两端同源。

| 顺序 | 缺口                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 代价                   |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- |
| G1   | ✅ **已收口**（同日）：`requirements.md:208` 与 `development-plan.md:100` 的「20 个 Demo 用例」已更正为 24                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | —                      |
| G2   | ✅ **已收口**（同日）：`transition-robustness.spec.ts` 用 `it.each` 逐个断六种 `animateName` 的 `okr-anim-<name>` 接线；`verify-dist.mjs` 的 CSS 侧由「只钉一组」改为逐个断 `enter-active` / `leave-active`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | —                      |
| G3   | ✅ **已收口**（同日）：新增 `tests/model/framework-boundary.spec.ts` 钉住模型层无 Vue 2 残留写法、且从 `vue` 只引入响应式 API                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —                      |
| G4   | ✅ **已收口**（同日）：`transition-robustness.spec.ts` 断点击 +/- 收起时切 `is-hidden` 且子树不卸载、再点恢复                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —                      |
| G5   | ✅ **已收口**（同日）：Q9 的 `.cjs` 解析路径现由 `verify-dist.mjs` 用 `createRequire` 真实加载并断言 3 项，`"type": "module"` 包最容易静默回归的那条路径已有门禁守着                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | —                      |
| G6   | ✅ **已收口**（同日）：新增 `tests/api-surface.spec.ts`（对齐 react 的 `tests/api-surface.spec.tsx`），表→实例、实例→表、行数与成员数三向防漂移。**它当场查出并修掉一处真实缺陷**：`defineExpose` 有 `getNodeKey`，而 `shared/api.ts` 的 Methods 表没有这一行，于是 README、`docs-site/api`、playground 的 API 页三处同时查不到这个方法（react 侧审计 §6.3 早指出、vue3 未修）。同时补了 `store / root` 一行                                                                                                                                                                                                                                                                                                                                                                                                | —                      |
| G7   | ✅ **已收口**（同日）：`BUILT_IN_THEMES` 已在 `src/lib/index.ts` 补导出，与 react 端导出面对齐；属新增对外 API，已记 `CHANGELOG.md` 的 Unreleased 段（已随 1.14.0 定版）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | —                      |
| G8   | ✅ 已收口（2026-09-25）：新增 `tests/visual/demo-interaction.spec.ts`，22 条真实浏览器用例把 24 个 Demo 的**交互结果**逐个钉住，一张 png 都没加（视觉用例 21 → **43** 条，基线仍 28 张）。判据一律是状态类与计算样式：`expanded` / `is-hidden` / `is-loading` / `is-checked` / `is-indeterminate` / `is-current` / `align-root` / `okr-anim-*` / `--okr-anim-duration` / svg 的 `path[d]` / `.okr-viewport-canvas` 的 `transform`；每条用例自带 console 与 pageerror 守卫。九条变异各有归属（另有两条单点变异结构性打不红，见本节末）。本批当场查出并修掉 Demo 自身四处缺陷：见本节末「G8 · Demo 自己不会说谎的四处缺陷」                                                                                                                                                                                   | 一批交互用例，代价不小 |
| G9   | ✅ 已收口（2026-09-24 第 3 轮第二批）：新增浏览器级用例 `tests/visual/umd.spec.ts` —— 真实 Chromium 里 `setContent` 内联 `dist/style.css` + `addScriptTag` 依次注入 `vue.global.prod.js` 与 `dist/vue3-okr-tree.umd.js`，挂载一棵 `connector="svg"` 的树，断 `window.VueOkrTree` 的具名导出、`[role="tree"]` / 4 个 `[role="treeitem"]` / `.okr-connector-svg path` 计数 > 0 且 `d` 非空、feishu 主题下卡片 `border-radius: 8px`（证 CSS 真的进了文档）、`pageerror` 与 `console.error` 为空。五条变异改 **UMD 产物本身**（`dist/` 是 gitignored 构建输出，逐字还原）：全局导出名改掉、具名 `OkrTree` 丢失、`role="treeitem"` 改名、`.okr-connector-svg` 类名改掉、把 `dist/style.css` 挪走 —— 五体全部以断言失败打红（驱动 `_scratch/probe-umd-mutation-vue3.mjs`）。视觉套件 20 → **21** 条，png 仍 28 张 | 一个 Playwright 用例   |
| G10  | ✅ 已收口（2026-09-24 第 3 轮第二批）：两种锁定强度实测分离，结论与踩坑见本节末尾那段（已按实测改写），`controlled.spec.ts` 补三条用例钉住，三条变异各有归属                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 一条探针               |

剩余待办：~~**G8**（24 个 Demo 的交互层自动化断言）不是收口类的小活，建议排期而非塞进本批~~ → **G8 已于 2026-09-25 收口**（见上表与本节末三段）；**G9 / G10** 均已在第 3 轮第二批收口。另留一处未决不对称：`DEFAULT_PROPS` 仍是 react 导出、vue3 未导出（非缺陷，需要时再定）。

**跨实现形状比对的门禁不在本仓，在姊妹仓**：`react-okr-tree/packages/react-okr-tree/tests/visual/cross-impl.spec.ts`（DOM 结构 + 内联样式，10 个模式）与 `cross-impl-svg.spec.ts`（`connector="svg"` 的 path `d`，5 个模式）拿本包 `pnpm build` 后在真实 Chromium 里捕获的 outerHTML / `d` 列表当夹具，比对的对手是 react 侧现算的结果，两套仓的 CI 互相看不到对方产物，夹具靠人工刷新（`pnpm gen:cross-impl`，需同机有两仓）。**因此它守不住本包单侧漂移**：本包改了 DOM 形状 / 状态类名 / svg 几何而没人刷新夹具时，两侧 CI 都照样全绿。所以本包这类改动的收尾步骤里要加一条——回 react 仓跑一次 `pnpm gen:cross-impl` 再看门禁，红了先判断哪边是对的，别直接刷新基线。**2026-09-25 的 Demo 交互批次已按这条收尾步骤跑过一次 `pnpm gen:cross-impl`**：15 个模式的载荷逐字未变，只有 `version`（1.14.0 → 1.14.1）与 `capturedAt` 两行元数据刷新，即那批改动（含画布平移收尾的库修复）对 DOM 形状与 svg 几何零漂移。

**CSS 几何常量门禁落在两仓的 `verify:dist`（react 侧清单 G17 的轻量分支）**：本仓 `scripts/verify-dist.mjs` 的 CSS 段与 react 端各 33 条、逐条同序同字面（唯一差别是消息里内插的实测数字）。钉的是相互咬合的那批几何值：六个变量（`--okr-gap-level:20px` 等）断「每处使用都带兜底 + 兜底值处处同值」，左子树短头 `12px` / `calc(100% - 11px)` / `10px` 与两处 `-1px` / `!important` 修正断**恰好出现一次**，`okr-unstyled` 断**五类**选择器，另断 `.is-measuring` 排在 `.is-measured` **之前**（组对齐修复的承重前提就是这条同特异度顺序，顺序一颠倒则 `measure()` 怎么改都会读回被钉住的宽度）。**为什么不做跨仓逐字 diff**：两仓 `src` 的 style.css 实测只差头注释与 `@import` 路径两处、`transition.css` 全等，但产物由两套压缩器各写一遍（本仓 esbuild、react 走 rolldown 内置那套），会合并同声明体的相邻规则、重排声明、`transparent`→`0 0`、`.3s height`→`height .3s`、颜色折成 `#fffffff0`——逐字比会假红一片，比不了。五条变异两仓各跑一遍全部打红（兜底值 `20px→24px`、两条规则顺序颠倒、五类降四类、`-11px→-12px`、`12px→14px`），探针在仓库外 `okr-audit-reports/_scratch/probe-css-mutation*.mjs` 与 `probe-css-gate-parity.mjs`。本仓 `verify:dist` 41 → **62** 条。

**拖拽悬停的渲染判据（react 侧 G9 的对称面）**：新增 `tests/components/local-update.spec.ts`，按事件顺序断「被写了 class 的元素」恰好是撤下与挂上那两个 label —— dragstart 0 处、首次悬停 1 处、同节点同分区重复 dragover 0 处、换目标 2 处、同节点换分区 1 处、dragEnter 0 处、dragend 1 处，五条变异各打红一次（驱动 `_scratch/probe-drag-mutation-vue3.mjs`）。**判据落点与 react 不同是机制决定的**：react 每节点订阅自己的版本号、断「谁被 bump」，本包的拖拽指示是 `OkrTree.vue` provide 下来的 `shallowRef`，18 个节点的 `labelWrapperClass` 都读它——实测一次换目标全员重新求值，只有值真变了的 2 个元素被写入 DOM（计数探针 `_scratch/probe-recompute-count.mjs`）。因此本包钉的是**DOM 写入落点**这条可观察契约；要把求值也收敛成局部得改 provide 形状（每节点一个指示 ref），属实现面改动，本轮不动，只补断言。另记一条 jsdom 陷阱：`MutationObserver` 回调按微任务派发，`await nextTick()` 后直接 `takeRecords()` 会随机拿到空数组，第一版探针因此把整棵树的写入读成 0 条，差点写成一条永真的假门禁。

**OKR 根卡片的水平坐标不变量（react 侧 G14 的对称面）**：`tests/visual/visual.spec.ts` 新增一条非像素的几何断言 —— 根卡片相对 `.org-chart-container` 的水平偏移在「收起右子树 → 展开 → 收起左子树 → 展开」四次读取上一字不动，每步同时断 `is-hidden` 的确切出现与消失。视觉套件因此 18 → **19** 条，**png 一张没加**（仍是 28 张，本条只读坐标不截图）。射程实测过、别夸大：`flex:1 1 0` → `0 0 auto`、根节点 `width:100%` → `auto` 这两条静态形状变异本条与两张 OKR 基线都抓不住，是 `group-align.spec.ts` 把它们打红的；本条独有的是「收起把子树从布局里拿走」（补一条 `.is-hidden{display:none}` 即红）。变异改的是 `playground/dist` 里的产物 CSS，跑完逐字还原。

**焦点环的计算样式断言（react 侧 G13 不依赖像素那一半的对称面）**：`tests/visual/visual.spec.ts` 新增一条键盘可达 + 计算样式用例——`Tab` 循环走到第一个 `.org-chart-node`（80 次走不到即判失败，防后面的环断言空转），断**卡片本体**（`:scope > .org-chart-node-label > .org-chart-node-label-inner`）的 `outlineStyle='solid'` / `outlineWidth='2px'`，同时断聚焦元素自身是 `none`（`:focus{outline:none}` 那条重置）。这条的价值一半在「读哪个元素」：直觉会去读聚焦元素，而 `style.css:686-695` 把环挂在其子孙上，照直觉写会得到一条假红（姊妹包探针实测）。四条变异两仓各打红一次，其中「去掉 `:focus-visible` 让环常驻」是专门反自证的那条——只有「Tab 走开后环必须收掉」抓得住它。视觉用例 19 → **20** 条、png 仍 28 张（本条只读计算样式，不截图）。react 侧 G13 剩下的 `orthogonal` / `straight` 形状是像素基线的事，不属本轮无副作用范围。

**G10 · 锁定态的第二种强度已验出来（第 3 轮第二批收口）**：`controlled.spec.ts` 新增的锁定态用例已确认「只传 `expanded-keys`、不接 `update:expanded-keys`」**不冻结视图** —— 渲染源是 store，prop 只在创建期与宿主传入值变化时回灌，宿主不回写就没有第二次同步，点击照常折叠（react 侧同样三种写法逐字测过，结论一致，所以不是复刻偏差）。但 react 文档站 `guide/controlled.mdx:91-93` 另有一档说法：「`expandedKeys` 写成每次渲染新建的数组字面量 → 完全锁定；引用稳定的数组 → 交互结果保留到 prop 真的变化为止」。**这一档在第二批验成了，说法成立**：上一批没分出来是因为宿主重渲染时 `data={makeData()}` 同时也换了引用，触发整树重建，两条路径混在一起（两个分支的观测结果逐字相同，正是被整树重建主导的那种「相同」）。把 `data` 提成稳定引用后重测，本包三条分支实测为：① 内联字面量 ⇒ 点完收起、宿主一重渲染就被拉回展开（完全锁定）；② 引用稳定的普通数组 ⇒ 折叠结果保留，且**原地 push 内容不算变化**——非响应式数组没有任何东西可通知 Vue，watcher 的 `deep: true` 也就挂不上依赖；③ 数组是 `ref` 包着的响应式对象 ⇒ 原地 push **算**变化、照样回灌。所以「引用稳定 = 保留到 prop 真的变化为止」里的“变化”，实际等价于「换引用，或由 `ref` 包着的数组改内容」。三条用例已进 `controlled.spec.ts`；变异三条：摘掉受控 watcher（三条新用例全红，原有两条「不回写即不冻结」保持绿）、摘 `deep: true`（只红第 ③ 条，那正是这个选项唯一的价值）、加一条「每次 `onUpdated` 无条件按 prop 回灌」（**打不红**，且是结构性的：prop 引用没变时子组件根本不被更新，库内构造不出打破它的变异——所以第 ② 条不是某行代码写对了，是 Vue 的 prop 比较 + 更新跳过共同保证的）。姊妹包 react 侧同批测同样三条分支、观测逐字一致，但它那边第 ② 条**有**对应变异可打红（`useEffect` 依赖换成 `[...expandedKeys]` 即按内容比），机制差记一笔。本包文档此前没有这条表述，故只补用例与本段，未改任何对外措辞。

**G8 · Demo 交互层批次（2026-09-25 收口）**：`tests/visual/demo-interaction.spec.ts` 22 条用例覆盖 24 个 Demo（`demo-1+2`、`demo-4+5` 各合用一条），全部只读状态类与计算样式，不截图像素。九条变异各有归属：`defaultExpandedKeys` 的父链展开（`node.ts` 的 `expand(true)`→`expand(false)`）、折叠圆盘数字（`showRightBtnText` 恒 false）、平移吞点击的标志位不复位、`accordion` 不收起同级、Demo 过滤函数对空值返回 false、Demo 的 `treeRef` 绑定摘掉、`is-left-child-node` 不下发、`allow-drag` 失效——各打红恰好一条用例。
**两条单点变异结构性打不红，别误判成假门禁**：① 「`check-strictly` 被无视」需要两处同改（`handleCheckToggle` 传的 `deep` 与 `setChecked` 内部 `if (this.store.checkStrictly)` 的短路，任一单独改掉都不可观察）；② 「`allow-drag` 被无视」同理（`draggable` 属性与 `handleDragStart` 的守卫是两道独立闸门，浏览器在属性那一层就止住了拖动）。这两条契约由两道守卫共同保证，组合变异才能打红，驱动脚本 `_scratch/probe-g8-mutations-vue3-c.mjs` 已验过。

**G8 当场查出的四处 Demo 缺陷（本批已修，都在 playground 侧，不动库与产物）**：

1. `BaseCheckbox.vue` 声明了 `treeRef` 却没绑到 `<VueOkrTree>` 上，工具条两个按钮（`setCheckedKeys` / `getCheckedKeys`）点了毫无反应，而日志仍打出一行「勾选 [7, 8]…」——**一个静默的空操作，外加一句谎**。
2. 同一行日志的文案称「父节点 6 半选」，而 `[7, 8]` 恰是 6 的全部子节点 ⇒ 6 是**全选**，半选的是根 1。文案按实测改成「6 全选、根 1 半选」。
3. `Base081.vue`（demo-12）的说明称 `show-node-num`「配合 `node-btn-content` 时显示自定义按钮内容」，而模板里 `showNodeNum` 分支在前，`renderBtnContent` 永不执行——该函数是死代码。react 侧对应的 `expand-btn` 用例文案写的是真话（`showNodeNum 优先，两个自定义口完全不会被调用`），本仓文档站因此比姊妹包多说了一半假话。
4. ✅ `node-drag-end` 的载荷（**同日第三批已修**）：`handleDrop` 在 `dragend` 之前清空 `dragOverNode` / `dragOverType`，于是成功放置后 `onDragEnd` 恒收 null，`BaseDraggable.vue` 每次成功拖动都记一行「未完成放置」。react 侧同一条路更彻底：跨父级移动会把源元素卸载重建（React 的键只在同一父级内去重），浏览器那次 `dragend` 落在已脱离文档的节点上，宿主侧**收不到这条事件**（jsdom 里元素被重新查询、事件照到，所以只有浏览器能看出这半边）。修法统一成「drop 里就地补发并收尾」：`node-drop` 之后立刻发 `node-drag-end`（载荷与之一致）并清 `draggingNode`，真到达的 dragend 被守卫短路只清状态；取消/被拒那一路仍报 null。

**顺带实测到的三条库层面观察**（都不是 Demo 层问题，动的是库语义；**同日第三批三条全部修完**，逐条给门禁与变异）：

- ✅ **画布平移把指针拖出 `.okr-viewport` 再松手会卡住**——**同日第二批已修并钉成门禁**。原状：组件只在根元素上挂 `pointerup`（既没有 `setPointerCapture` 也没有 window 级监听），于是 `handlePointerUp` 根本不执行——`is-panning` 类残留、`panStart` 不清，之后**不带按键**的悬停移动会继续拖着画布跑（实测 offset 从 `translate(125px, -379.5px)` 走到 `translate(290px, 0px)`）。修法是一个挂载期注册、卸载时摘掉的常驻 window 监听做收尾，且它不带 `armSwallow`（松手在画布外时浏览器不会在画布里派发 click，武装了会吃掉用户回来后的第一次正常点击）。门禁三条：`tests/components/viewport.spec.ts` 的「松手落在画布外：手势照样收尾…」与「画布外松手不武装吞点击…」，加上 `tests/visual/demo-interaction.spec.ts` demo-19 末尾那段真实浏览器悬停回归。变异各有归属：摘掉 window 监听 → 前两条与浏览器那条各自打红（`is-panning` 残留 / 偏移被悬停推动）；把收尾改成 `handlePointerUp(event, true)` → 「不武装吞点击」那条打红。react 侧同批同形同变异。
- ✅ **过滤词只命中 OKR 左子树时整棵树消失**（**同日第三批已修**）：如用关键字「左」匹配 `(左)销售部` 们，共用的根节点自身不匹配 ⇒ 被判不可见 ⇒ 连刚命中的左子树一起从 DOM 卸载（`getVisibleNodes()` 读 0）。两仓行为逐字一致，说明 Q1「父节点保持可见」那条修复只走通了右子树。现在左树命中也把根保住，门禁是 `tests/model/tree-store.spec.ts` 的「关键字只命中左子树时，共用的根节点保持可见」+ demo-14 的浏览器段；摘掉 store 里那一行 `rootNode.visible = true` 两处各自打红（浏览器侧表现为读回空数组，正是当初的症状）。承重前提写进了注释：OKR 下 `filter` 先右后左。
- ✅ **`default-checked-keys` 两仓分歧**（**同日第三批已修**）：两边都有按**引用**比较的运行时重放（react 是 `OkrTree.tsx` 的 effect，vue3 是 `OkrTree.vue` 的 watch + `store.setDefaultCheckedKeys`），所以宿主每次渲染新建等值数组都会先清空全部勾选再重放列表，把用户刚改的整片抹回去；react 文档站的 checkbox demo 因行内字面量而整页勾选点不动就是这个（vue3 demo 恰好没触发：它的日志状态在子组件里，点勾选不重渲染宿主）。现在两仓统一为**按内容比较**（`isSameKeyList`，`String(key)` 归一去重，与 `getCheckedKeys` 同口径），API 表与两站措辞同步更正；变异 = 摘掉 store 里那行守卫，两仓各有一条 checkbox 用例打红。

## 7. 本文用到的实跑命令

```bash
pnpm build && pnpm verify:dist          # 62 条 assert，含 3 条 .cjs 真实 require、7 条可选 peer 门禁、21 条 CSS 几何常量
pnpm test && pnpm test:coverage && pnpm size && pnpm verify:package
node -e "console.log(Object.keys(require('./dist/vue3-okr-tree.cjs')).length)"
node -e "console.log(Object.keys(require('./dist/vue3-okr-tree.cjs').VueOkrTree.props).sort().join('\n'))"
pnpm verify:dist 2>&1 | grep -c '^\[verify:dist\] ok:'   # 实跑 62 条
node --input-type=module -e "const {methodsSection}=await import('./shared/api.ts');console.log(methodsSection.rows.length)"
pnpm vitest run tests/api-surface.spec.ts tests/model/framework-boundary.spec.ts
find tests/visual -name '*-win32.png' | wc -l; find tests/visual -name '*-linux.png' | wc -l
grep -rn "is-hidden" tests/ --include="*.spec.ts"        # 收口前只有一处注释命中
grep -nE "require\(|createRequire|\.cjs" scripts/verify-dist.mjs   # 收口前零命中
gh run list -R baiwumm/vue3-okr-tree --workflow=release.yml --limit 1
```
