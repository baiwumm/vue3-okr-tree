# API

以下表格由 [`shared/api.ts`](https://github.com/baiwumm/vue3-okr-tree/blob/main/shared/api.ts) 单一来源生成：Playground、本文档站与 README 使用同一份数据。

<ApiDoc :section="attributesSection" />
<ApiDoc :section="propsSection" />
<ApiDoc :section="eventsSection" />
<ApiDoc :section="methodsSection" />
<ApiDoc :section="slotsSection" />
<ApiDoc :section="groupSection" />

## OkrTreeViewport 的 API

`<OkrTreeViewport>` 的 props / 方法见[画布缩放](/guide/viewport#props)一节。

<script setup lang="ts">
import ApiDoc from '../components/ApiDoc.vue'
import {
  attributesSection,
  propsSection,
  eventsSection,
  methodsSection,
  slotsSection,
  groupSection,
} from '../../shared/api'
</script>
